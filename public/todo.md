
-Add a new "Reminders" tab where users are able to set up a reminder with a clock icon. upon clicking on it, the user can select the time where the alarm goes off, the name of the lead, and the name of the client. The user will get an alarm sound when the time elapses.
-Add a questions tab with the respective input boxes
-Firebase domain has a different layout from the localhost live server (share screenshots)
